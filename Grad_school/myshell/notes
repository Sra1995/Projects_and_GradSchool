The function display_not_allowed_message uses fprintf instead of printf to print the message to the standard error stream (stderr) instead of the standard output stream (stdout).

Using fprintf with stderr allows the error message to be displayed separately from the regular program output. This can be useful for distinguishing error messages from normal program output, especially when the program's output is being redirected or piped to another process.